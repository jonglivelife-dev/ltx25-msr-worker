# LTX-2.5 + MSR — RunPod serverless worker

`runpod/worker-comfyui` plus `ComfyUI-LTX2.5-MSR`, pointed at a network volume
for its models. Custom nodes cannot be loaded from a volume — that is the only
reason this image exists.

Built by GitHub Actions to `ghcr.io/<owner>/ltx25-msr-worker:latest`.
